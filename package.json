{
  "name": "esm-drupal-example",
  "version": "1.0.0",
  "description": "",
  "main": "esbuild.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && node ./esbuild.js"
  },
  "keywords": [],
  "author": {
    "name": "Adrian Ortega",
    "email": "hello@adrianortega.dev"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.18.15",
    "esbuild-sass-plugin": "^2.10.0",
    "rimraf": "^5.0.1"
  }
}
